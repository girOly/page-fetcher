const args = process.argv.slice(2);
const fs = require('fs');
const request = require('request');
request = function(args[1], (error, response, body)
fs.writeFile(args[0], body, error {
  if (error) {
    return (error);
  }
  let bigStuff = statSync.size;
  let statSync = fs.statSync(args[1]);
  console.log(`Downloaded and saved ${bigStuff} bytes to ./index.html`);

});
});
